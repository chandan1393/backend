# Server
server.port=8080

# Timezone — backend always stores and operates in UTC
# Each user's browser converts UTC to their local timezone automatically
spring.jackson.time-zone=UTC
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSS'Z'
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.adjust-dates-to-context-time-zone=false

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://localhost:5432/assignease_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=ass
app.jwt.refreshExpiration=60

# Mail (Gmail SMTP — use App Password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chandansharma1393@gmail.com
spring.mail.password=azee tfzi mbyj yjtqv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=uploads/

# Stripe (manual payment links — no webhook needed)
# Admin creates payment links at https://dashboard.stripe.com/payment-links
# Paste those links into each installment in the admin dashboard
stripe.secret.key=dsfsf
stripe.webhook.secret=whsec_YOUR_WEBHOOK_SECRET
app.frontend.url=http://localhost:4200

# Twilio WhatsApp (for payment reminders)
# Get credentials at https://console.twilio.com
# Use Sandbox for testing: https://console.twilio.com/us1/develop/sms/try-it-out/whatsapp-learn
twilio.account.sid=YOUR_TWILIO_ACCOUNT_SID
twilio.auth.token=YOUR_TWILIO_AUTH_TOKEN
twilio.whatsapp.from=whatsapp:+14155238886


# Razorpay
razorpay.key.id=zxczc
razorpay.key.secret=zxczxc

# Company config
app.company.name=EduAssist

# CORS
app.cors.allowed-origins=http://localhost:4200

# Logging
logging.level.com.assignease=DEBUG
logging.level.org.springframework.security=INFO
